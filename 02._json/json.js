const test =
{
    color: "Red",
    rating: 0
};

console.log( test);
console.log( test.color);
console.log( test.rating);

const tests = {
    colors: [
        {
            color: "Blue",
            rating: 0
        },
        {
            color: "Yellow",
            rating: 0
        }
    ]
};

console.log( tests.colors);
console.log( tests.colors[1].color);

